Event sourcing experiments
============

Playing around with a CRUD and an event-sourced version of a dummy graph database. See TODO:link for an accompanying
blog post.

To get started, run one of the following commands:

* `./gradlew build` to run checks and tests
* `./gradlew idea` for IntelliJ
* `./gradlew eclipse` for Eclipse
